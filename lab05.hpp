#include <iostream>
#include <algorithm>
#include <vector>
#include <string>
using namespace std;
float mean(const vector<float>&);
pair<float, float> minMax(const vector<float>&);
int argmax (const vector<float>&);
void sort(vector<float>&);
bool remove_first_negative_element(const vector<int>& vec, int& removed_element);
string replace(const string& str, const string& old, const string& new_string);
std::vector<std::string> split(const std::string&, char sep);
std::string join(const std::vector<std::string>&, const std::string& sep);





